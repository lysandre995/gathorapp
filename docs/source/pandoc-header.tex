% Package necessari
\usepackage{graphicx}
\usepackage{fontspec}

% Imposta il font principale come monospace
% Usa Courier New che è sempre disponibile su Windows
\setmainfont{Courier New}
\setmonofont{Courier New}

% Altri font monospace alternativi
% \setmainfont{Consolas}          % Windows, molto leggibile
% \setmainfont{Lucida Console}    % Windows classico
% \setmainfont{DejaVu Sans Mono}  % Se installato separatamente

% Migliora la spaziatura per il testo monospace
\usepackage{setspace}
\setstretch{1.15}

% Evita problemi con caratteri speciali
\defaultfontfeatures{Ligatures=TeX,Scale=MatchLowercase}
